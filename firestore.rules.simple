rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Temporary simple rules - allow all authenticated users
    // Use this if you're having offline/permission issues
    
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
